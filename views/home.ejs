<%- include("partials/header"); %>

<h1>Home</h1>
<p> <%= homeStartingContent %> </p>

<% blogPosts.forEach(function(blogPost, index){ %>
  <div>
    <h5><%= blogPost.title %></h5>
    <p><%= blogPost.post.substr(0, 100) + "..." %> <a href=<%= "/posts/" + index;%>>Read More</a></p>
  </div>
<% }); %>

<%- include("partials/footer"); %>
